[pytest]
testpaths = tests
# Home Assistant's own convention: every test coroutine runs without needing a
# marker, which is what pytest-homeassistant-custom-component expects.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
